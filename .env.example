TURBO_TOKEN=your-secret-token
CACHE_DIRECTORY=./cache
PORT=3000

# LRU cache eviction (0 = disabled)
# MAX_CACHE_SIZE=5000    # Max cache size in MB (e.g., 5GB)
# MAX_CACHE_COUNT=1000   # Max number of artifacts
# EVICT_BATCH=10         # Number of artifacts to evict per cleanup
